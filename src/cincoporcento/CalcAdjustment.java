package cincoporcento;

public class CalcAdjustment {

    public static void calcFivePercent(double value) {
        double adjustedValue = value + (value * 0.05);
        System.out.println("Result: " + adjustedValue);
    }

}
